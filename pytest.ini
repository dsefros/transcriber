[pytest]
testpaths = tests
addopts = --strict-markers
pythonpath = .
markers =
    unit: fast hermetic tests with fully controlled local fixtures.
    integration: broader runtime or cross-boundary tests that still avoid external services.
    smoke: focused end-to-end-ish checks for canonical pipeline contracts.
