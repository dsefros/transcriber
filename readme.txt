1. Канонический способ запуска (РЕКОМЕНДУЕМЫЙ)
▶️ Полная обработка аудио через Job → Worker → legacy
python -m src.cli_v2 data/raw/audio1883612068.m4a
Что происходит:
создаётся Job
Worker принимает job
JobRunner запускает legacy pipeline
выполняется:
транскрибация
анализ
запись в БД
файлы (output/*.md, output/*.json)

2. Полная обработка БЕЗ БД (через legacy, но управляемо)
python -m src.cli_v2 data/raw/audio1883612068.m4a --no-db
(если ты пробрасываешь --no-db в legacy через Job — если нет, пока используй вариант ниже)

3. Переанализ существующего JSON (Job → legacy)
python -m src.cli_v2 data/raw/audio1883612068.json --json
Что происходит:
транскрибация пропускается
сегменты берутся из JSON
выполняется только LLM-анализ
обновляются .md / .json
БД не используется

4. Запуск legacy CLI напрямую (ДОПУСТИМО, но НЕ ОСНОВНОЙ)
⚠️ Используется только для отладки legacy, не для прод-логики.
python -m src.legacy.cli data/raw/audio1883612068.m4a --device cuda --no-db
или:
python -m src.legacy.cli --json output/audio1883612068.json
📌 Отличие:
нет Job
нет Worker
нет защиты от sys.exit()
это «ручной режим»

5. Проверочные / диагностические команды (обязательно сохранить)
Проверка, что пакетная структура жива
python -c "import src; print('src OK')"
python -c "import src.legacy.pipeline.main; print('legacy OK')"
python -c "import src.worker.worker; print('worker OK')"
Проверка, что cli_v2 запускается как модуль
python -m src.cli_v2 --help
Проверка защиты от падений (sys.exit)
python -m src.cli_v2 no_such_file.wav
Ожидаемо:
status: failed
error: Legacy called sys.exit(1)